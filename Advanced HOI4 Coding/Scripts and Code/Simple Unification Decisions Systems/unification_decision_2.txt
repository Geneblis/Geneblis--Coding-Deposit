your_unification_category_or_smth = {
	unification_decision_using_countries = { #Adds cores of your nation (ROOT) and removes the previous owner (FROM)
		visible = {
			OR = { #For multiple tags
				original_tag = TAG1
				original_tag = TAG2
			}
		}
		target_root_trigger = {
			#original_tag = TAG1
			#You can add any trigger you desire, or even none, aka, always = yes
		}
		targets_dynamic = yes
		target_non_existing = yes
		target_trigger = {
			FROM = {
				any_core_state = {
					AND = {
						is_core_of = FROM 
						is_owned_by = ROOT
						NOT = {is_core_of = ROOT}
					}
				}
			}
		}
		cost = 25
		modifier = {
			political_power_gain = -0.15
		}
		remove_effect = {
			FROM = {
				every_core_state = {
					limit = {
						AND = {
							is_core_of = FROM 
							is_owned_by = ROOT
							NOT = {is_core_of = ROOT}
						}
					}
					add_core_of = ROOT
					remove_core_of = PREV
				}
			}
		}
		days_remove = 30
		ai_will_do = {
			factor = 10
		}
	}
}